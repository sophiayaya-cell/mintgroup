---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProductGrid from '../../components/ProductGrid.astro';
import { UI } from '../../i18n/ui';
const locale = 'de';
const t = UI[locale];
---
<BaseLayout locale="de" title={t.productsTitle}>
  <ProductGrid locale="de" />
</BaseLayout>
