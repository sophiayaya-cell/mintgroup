---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ProductGrid from '../../components/ProductGrid.astro';
import { UI } from '../../i18n/ui';
const locale = 'zh';
const t = UI[locale];
---
<BaseLayout locale="zh" title={t.productsTitle}>
  <ProductGrid locale="zh" />
</BaseLayout>
